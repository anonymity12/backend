archOfPostCommentGoldSystem.txt
- comment: addComment: + score for post
					 : - gold for commenter
					 : + gold for post owner;
- post: addPost: + gold for post owner; URL：/add
        * watchPost: +gold for owner; URL: /detail/postId
                   : - gold for watcher
                   : +userInfo
                   : +likeCount <- likeService.findEntityLikeCount(postId)
                   : +likeStatus<- likeService.findEntityLikeStatus(curUserId, postId)
                   : +comments (commentService.findCommentByEntity(type:post/comment, entityId, ))
                        : for this post
                        : for some comment
                      - comment(conent)
                      - user(comment owner)
                      - likeCount
                      - likeStatus
                      - replyCount
                      - replys
                          + reply
                          + user
                          + target
                          + likeCount
                          + likeStatus
        * topPost置顶帖子：updateType/updateStatus(id, type/status)
- rankList of post:
	sorted by score;
